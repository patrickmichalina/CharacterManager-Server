# Minimal Web API Starter
This is a very stripped down version of Web API. The standard visual studio templates contain MVC code that is unnecessary when only building an API for things like Angular.
